import React from "react";

export const TopBar = () => (
  <div className="home-topbar">
    <img src="img/logo.svg" alt="Logo" className="home-logo" />

    <a href="#" className="button button-alt">
      Kontakt
    </a>
  </div>
);
